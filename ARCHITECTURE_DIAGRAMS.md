# 🎨 Diagramas Visuais da Arquitetura

## 1️⃣ Fluxo de Dados Geral

```
┌────────────────────────────────────────────────────┐
│                    App.tsx                         │
│              (Container/Orquestrador)              │
│                                                    │
│  const cart = useCartFacade();                     │
│  const events = EVENTS;                            │
└────────┬────────────┬────────────┬────────────────┘
         │            │            │
         │            │            │
    ┌────▼────┐ ┌────▼────────┐ ┌─▼──────────────┐
    │ Header  │ │ EventsList  │ │  CartDrawer    │
    │ (UI)    │ │ (UI)        │ │  (UI)          │
    │         │ │             │ │                │
    │ eventos:│ │ ├─EventCard │ │ items: []      │
    │ 🛒      │ │ │           │ │ total: 0       │
    │         │ │ └─ usar API │ │ onRemove()     │
    └────┬────┘ │   eventos   │ └────┬───────────┘
         │      └─────┬───────┘      │
         │            │              │
         └────────┬───┴──┬───────────┘
                  │      │
              ┌───▼──────▼────┐
              │  CurrencyService│  ← Formatação
              │  CurrencyService│
              └────────────────┘
                       │
                       ▼
          ┌─────────────────────┐
          │  useCartFacade()    │
          │                     │
          │ ├─ getTotalItems()  │
          │ ├─ getTotalPrice()  │
          │ ├─ toggleCart()     │
          │ └─ removeItem()     │
          └──────────┬──────────┘
                     │
                     ▼
          ┌──────────────────────┐
          │  useCartStore()      │
          │  (Zustand)           │
          │                      │
          │ - items[]            │
          │ - isOpen boolean     │
          │ - addToCart()        │
          │ - removeFromCart()   │
          └──────────────────────┘
```

---

## 2️⃣ Padrão Facade em Detalhes

### SEM Facade ❌

```
┌──────────────────────┐
│      App.tsx         │
│                      │
│ const {              │
│   items,             │ ◄─── Precisa saber TUDO
│   toggleCart,        │      sobre Zustand
│   isOpen,            │
│   removeFromCart,    │
│   totalPrice,        │
│   addToCart          │
│ } = useCartStore();  │
│                      │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│  Zustand Store       │
│  (Complexo)          │
│                      │
│ ├─ estado            │
│ ├─ ações             │
│ ├─ seletores         │
│ └─ getters           │
└──────────────────────┘

❌ App acoplado ao Zustand
❌ Difícil testar
❌ Difícil mudar de biblioteca
```

### COM Facade ✅

```
┌──────────────────────┐
│      App.tsx         │
│                      │
│ const cart =         │ ◄─── Interface clara
│   useCartFacade();   │      e simples
│                      │
│ cart.getTotalItems() │
│ cart.getTotalPrice() │
│ cart.toggleCart()    │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────────┐
│    CartFacade.ts         │
│  (Interface Simples)     │
│                          │
│ ├─ getTotalItems()       │
│ ├─ getTotalPrice()       │
│ ├─ toggleCart()          │
│ ├─ removeItem()          │
│ └─ addItem()             │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────┐
│  Zustand Store       │
│  (Detalhe interno)   │
│                      │
│ ├─ estado            │
│ ├─ ações             │
│ ├─ seletores         │
│ └─ getters           │
└──────────────────────┘

✅ App desacoplado do Zustand
✅ Fácil testar
✅ Fácil mudar de biblioteca
```

---

## 3️⃣ Padrão Service Layer em Detalhes

### SEM Service ❌

```
┌─────────────────────┐
│   App.tsx           │
│                     │
│ const formatCurrency│ ◄─── Lógica aqui
│   = (v) => new      │
│     Intl...         │
└─────┬───────────────┘
      │
      │ Duplicado em:
      │
      ├─ EventCard.tsx
      │  (mesma lógica)
      │
      └─ CartDrawer.tsx
         (mesma lógica)

❌ Duplicação
❌ Se mudar, muda em 3 lugares
❌ Difícil manter
```

### COM Service ✅

```
┌──────────────────────┐
│  App.tsx             │
│                      │
│ CurrencyService      │ ◄─── Usa Service
│   .format(price)     │
└──────────┬───────────┘
           │
           │
├──────────┼──────────┐
│          │          │
▼          ▼          ▼

EventCard CartDrawer  (outros)
 .format   .format     .format

    ▲      ▲           ▲
    └──────┼───────────┘
           │
┌──────────▼──────────────┐
│ CurrencyService.ts      │
│                         │
│ format(value: number)   │
│   return Intl...        │
└─────────────────────────┘

✅ Lógica em 1 lugar
✅ Reutilizada em 5 lugares
✅ Se mudar, muda em 1 lugar
✅ Fácil manter
```

---

## 4️⃣ Padrão Container/Presentational

```
┌──────────────────────────────────────────────┐
│  App.tsx (CONTAINER - Smart Component)       │
│                                              │
│  Responsabilidades:                          │
│  ✅ Acessa estado (CartFacade)               │
│  ✅ Lógica de negócio                        │
│  ✅ Orquestra componentes filhos             │
│  ✅ Passa dados via props                    │
│                                              │
│  const cart = useCartFacade();               │
│  return (                                    │
│    <>                                        │
│      <EventsList events={EVENTS} />          │
│      <CartDrawer                             │
│        items={cart.items}                    │
│        onRemove={cart.removeItem}            │
│      />                                      │
│    </>                                       │
│  );                                          │
└────┬──────────────┬─────────────────────────┘
     │              │
     │              │
┌────▼──────┐  ┌────▼──────────┐
│ EventsList │  │ CartDrawer    │
│            │  │               │
│ (Props):   │  │ (Props):      │
│ events ✅  │  │ items ✅      │
│ onAdd  ✅  │  │ onRemove ✅   │
│            │  │               │
│ ❌ NÃO:    │  │ ❌ NÃO:       │
│ - acessa   │  │ - acessa      │
│   estado   │  │   estado      │
│ - lógica   │  │ - lógica      │
│   complexa │  │   complexa    │
└────────────┘  └───────────────┘

Componentes Presentational
(Dumb Components)
- Recebem tudo via props
- Apenas renderizam
- 100% reutilizáveis
```

---

## 5️⃣ Fluxo de Cliques (Interação)

```
Usuário clica em "Comprar"
          │
          ▼
┌─────────────────────────┐
│ EventCard.tsx           │
│                         │
│ <button                 │
│   onClick={() =>        │
│     addToCart(event)    │ ◄─── Chama função
│   }                     │      recebida via props
│ >                       │
└──────────┬──────────────┘
           │
           ▼
┌──────────────────────────┐
│ App.tsx                  │
│                          │
│ <EventCard              │
│   event={event}         │
│   addToCart={ () =>      │
│     cart.addItem(event) │ ◄─── Passa função
│   }                     │
│ />                      │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│ CartFacade.ts            │
│                          │
│ addItem: (event) =>      │
│   store.addToCart(event) │ ◄─── Delega
│                          │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│ useCartStore (Zustand)   │
│                          │
│ addToCart: (event) =>    │
│   set((state) => {       │
│     // atualiza estado   │ ◄─── Atualiza estado
│   })                     │
│                          │
└──────────────────────────┘
           │
           ▼
    Renderizar novamente
    com novos dados
```

---

## 6️⃣ Hierarquia de Componentes

```
┌─────────────────────────────────────────┐
│                App.tsx                  │
│           (Único Container)             │
└──────────────┬────────────────────────┬─┘
               │                        │
        ┌──────▼────────┐        ┌──────▼─────────┐
        │  EventsList   │        │  CartDrawer    │
        │  (Apenas UI)  │        │  (Apenas UI)   │
        │               │        │                │
        │  ├─ map()     │        │  ├─ conditional│
        │  │   eventos  │        │  │ (if isOpen) │
        │  └─ render    │        │  ├─ map items  │
        │    EventCard  │        │  └─ buttons    │
        │               │        │                │
        └──────┬────────┘        └────────────────┘
               │
        ┌──────▼──────────┐
        │   EventCard     │
        │   (Apenas UI)   │
        │                 │
        │   ├─ image      │
        │   ├─ details    │
        │   ├─ price      │
        │   └─ button     │
        │                 │
        └─────────────────┘

Profundidade: 3 níveis
Componentes Presentational: 3 (EventsList, CartDrawer, EventCard)
Componentes Container: 1 (App)
```

---

## 7️⃣ Dependências e Injeção

```
┌─────────────────────────────────────┐
│         App.tsx (Container)         │
│                                     │
│ Cria as dependências:               │
│ - const cart = useCartFacade()      │
│ - const events = EVENTS             │
│                                     │
│ Injeta nos filhos via props:        │
│                                     │
│ <EventsList events={...} />         │
│ <CartDrawer items={...} />          │
└─────────────┬───────────────────────┘
              │
    ┌─────────┴─────────┐
    │                   │
┌───▼───────┐      ┌────▼──────┐
│ EventsList│      │CartDrawer  │
│           │      │            │
│ Recebe:   │      │ Recebe:    │
│ - events  │      │ - items    │
│ (prop)    │      │ - totalPrice
│           │      │ (props)    │
└───────────┘      └────────────┘

Padrão: Dependency Injection
Benefício: Componentes desacoplados
           Componentes reutilizáveis
           Fácil testar
```

---

## 8️⃣ Estados e Transições

```
┌─────────────────────────┐
│   Carrinho Fechado      │
│   (isOpen = false)      │
│                         │
│   🛒 Carrinho (0)       │
│                         │
│   ← Drawer não visível  │
└────────┬────────────────┘
         │
         │ Clique em "🛒 Carrinho"
         │ cart.toggleCart()
         │
         ▼
┌─────────────────────────┐
│   Carrinho Aberto       │
│   (isOpen = true)       │
│                         │
│   🛒 Carrinho (2)       │
│                         │
│   ← Drawer visível ✓    │
│   ┌─────────────────┐   │
│   │ Seu Carrinho    │   │
│   │  ├─ Item 1      │   │
│   │  └─ Item 2      │   │
│   │ Total: R$ 670   │   │
│   └─────────────────┘   │
└────────┬────────────────┘
         │
         │ Clique no "X"
         │ cart.toggleCart()
         │
         ▼
┌─────────────────────────┐
│   Carrinho Fechado      │
│   (isOpen = false)      │
│                         │
│   🛒 Carrinho (2)       │
│                         │
│   ← Drawer não visível  │
└─────────────────────────┘
```

---

## 9️⃣ Comparação de Arquitetura

### ANTES (Problema)

```
App.tsx
├── Header
├── EventsGrid (renderiza eventos aqui)
├── Drawer (renderiza carrinho aqui)
│
Problemas:
❌ App.tsx tem 151 linhas
❌ App.tsx faz tudo
❌ Difícil ler
❌ Difícil testar
❌ Difícil estender
```

### DEPOIS (Solução)

```
App.tsx (apenas orquestra)
├── <Header />
├── <EventsList />        ← Componente separado
│   └── <EventCard />     ← Componente separado
└── <CartDrawer />        ← Componente separado
    ├── CurrencyService   ← Lógica reutilizável
    └── CartFacade        ← Abstração de estado
        └── useCartStore  ← Estado real

Benefícios:
✅ App.tsx tem 52 linhas
✅ App.tsx apenas orquestra
✅ Fácil ler
✅ Fácil testar
✅ Fácil estender
```

---

## 🔟 Métricas Visuais

```
Linhas de Código

App.tsx:
Antes: ████████████████████░ 151 linhas
Depois:██████░                52 linhas
        Redução: 66%

Duplicação:
Antes: CurrencyService 3 vezes
       ███
Depois: CurrencyService 1 vez
        █

Testabilidade:
Antes: ⭐⭐ (Difícil)
Depois: ⭐⭐⭐⭐⭐ (Fácil)

Manutenibilidade:
Antes: ⭐⭐⭐ (Média)
Depois: ⭐⭐⭐⭐⭐ (Alta)
```

---

**Diagrama criado em:** 24 de Dezembro de 2025
**Status:** ✅ Arquitetura clara e visual
